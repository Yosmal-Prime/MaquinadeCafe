name: CI Pipeline

on:
  push:
    branches:
      - main  # Este pipeline se ejecutará cuando hagas un push a la rama "main"
  pull_request:
    branches:
      - main  # También se ejecutará cuando se abra un PR hacia "main"

jobs:
  build:
    runs-on: ubuntu-latest  # Puedes usar Windows o Mac si prefieres otro entorno

    steps:
      # 1. Verificar el código
      - name: Check out code
        uses: actions/checkout@v3

      # 2. Configurar .NET Core
      - name: Set up .NET Core
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0'  # Asegúrate de usar la versión correcta

      # 3. Restaurar dependencias (especificando el proyecto de pruebas)
      - name: Restore dependencies
        run: dotnet restore MaquinaDeCafeTests/MaquinaDeCafeTests.csproj

      # 4. Construir el proyecto (especificando el proyecto de pruebas)
      - name: Build
        run: dotnet build MaquinaDeCafeTests/MaquinaDeCafeTests.csproj --no-restore

      # 5. Ejecutar las pruebas (especificando el proyecto de pruebas)
      - name: Run tests
        run: dotnet test MaquinaDeCafeTests/MaquinaDeCafeTests.csproj --no-build --verbosity normal

      # 6. (Opcional) Publicar artefactos si es necesario
      - name: Publish
        run: dotnet publish -c Release -o ./output
